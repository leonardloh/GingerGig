---
phase: 06
plan: 06
title: Public end-to-end smoke test and demo handoff
type: execute
wave: 4
depends_on:
  - 06-03-PLAN.md
  - 06-04-PLAN.md
  - 06-05-PLAN.md
autonomous: false
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
  - DEPLOY-05
  - DEPLOY-06
  - DEPLOY-07
  - DEPLOY-08
  - DEPLOY-09
  - DEPLOY-10
files_modified:
  - backend/docs/DEPLOYMENT_SMOKE_TEST.md
  - backend/docs/DEPLOYMENT_AWS.md
  - backend/docs/DEPLOYMENT_ALIBABA.md
---

<objective>
Run the final judge-facing smoke test from the public CloudFront URL and capture a handoff checklist proving frontend, backend, database, audio/Transcribe/Qwen, CORS, and demo accounts work together. Block smoke execution until both `.planning/phases/04-voice-to-profile-pipeline/04-05-SUMMARY.md` and `.planning/phases/05-frontend-wiring-type-extensions/05-07-SUMMARY.md` exist.
</objective>

<threat_model>
## Security Threat Model

**Assets:** public demo URL, demo accounts, backend bearer tokens, audio flow, cloud spend.

**Threats and mitigations:**
- **False-positive deployment:** smoke test must start from the public CloudFront URL, not localhost.
- **Partial persona coverage:** smoke must include Siti, Amir, and Faiz quick-login paths.
- **Voice feature missed:** smoke must include en-US voice-to-profile and returned `ListingDraft`.
- **Budget not configured:** AWS budget alerts at $50/$100 must be confirmed before repeated smoke runs.
</threat_model>

<tasks>
  <task id="06-06-01" type="execute">
    <name>Create public smoke test runbook</name>
    <files>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
    </files>
    <read_first>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md (create if missing)
      - backend/docs/DEPLOYMENT_AWS.md
      - backend/docs/DEPLOYMENT_ALIBABA.md
      - .planning/phases/04-voice-to-profile-pipeline/04-05-SUMMARY.md (must exist before smoke)
      - .planning/phases/05-frontend-wiring-type-extensions/05-07-SUMMARY.md (must exist before smoke)
      - .planning/ROADMAP.md
      - .planning/REQUIREMENTS.md
      - frontend/docs/API_READY_MIGRATION.md
    </read_first>
    <action>
Create `backend/docs/DEPLOYMENT_SMOKE_TEST.md` with sections:
- `# Deployment Smoke Test`
- `## Public URLs`
- `## Preconditions`
- `## Smoke Flow`
- `## Browser Console Checks`
- `## Cloud Checks`
- `## Sign-Off`

Add placeholders:
- `CloudFront URL: https://<cloudfront-distribution-domain>`
- `Backend origin: https://<alibaba-ecs-or-slb-host>`
- `Audio bucket: gingergig-audio-<env>`
- `OSS bucket: <oss-provider-photo-bucket>`

In `## Preconditions`, include unchecked boxes for Phase 4 complete, Phase 5 complete, backend health green, frontend loaded from CloudFront, AWS budget alerts at `$50` and `$100`, DashScope key live, and WebSocket timeout >=300 seconds.

Add this exact block:
`Smoke execution is blocked until .planning/phases/04-voice-to-profile-pipeline/04-05-SUMMARY.md and .planning/phases/05-frontend-wiring-type-extensions/05-07-SUMMARY.md exist.`
    </action>
    <acceptance_criteria>
      - `test -f backend/docs/DEPLOYMENT_SMOKE_TEST.md` passes.
      - `rg "Deployment Smoke Test|CloudFront URL: https://|Backend origin: https://|\\$50|\\$100|>=300 seconds|Smoke execution is blocked until" backend/docs/DEPLOYMENT_SMOKE_TEST.md` finds all strings.
    </acceptance_criteria>
  </task>

  <task id="06-06-02" type="execute">
    <name>Document and run three-persona demo smoke flow</name>
    <files>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
    </files>
    <read_first>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
      - frontend/src/prototype/PrototypeApp.jsx
      - .planning/phases/05-frontend-wiring-type-extensions/05-02-PLAN.md
      - .planning/phases/05-frontend-wiring-type-extensions/05-03-PLAN.md
      - .planning/phases/05-frontend-wiring-type-extensions/05-04-PLAN.md
      - .planning/phases/05-frontend-wiring-type-extensions/05-05-PLAN.md
    </read_first>
    <action>
In `## Smoke Flow`, add this exact checklist:
1. Open `https://<cloudfront-distribution-domain>` in a clean browser session.
2. Click Siti quick-login and verify elder dashboard loads listings and bookings.
3. Sign out or reload to login screen.
4. Click Amir quick-login and verify requestor search lists providers.
5. As Amir, create a booking from a listing detail screen.
6. Sign in as Siti and accept the pending booking.
7. Sign in as Companion/Faiz and verify dashboard, alerts, and timeline load.

For each step, include columns: `Status`, `Expected`, `Observed`, `Evidence`.
    </action>
    <acceptance_criteria>
      - `rg "Click Siti quick-login|Click Amir quick-login|create a booking|accept the pending booking|Companion/Faiz|Status.*Expected.*Observed.*Evidence" backend/docs/DEPLOYMENT_SMOKE_TEST.md` finds all strings.
      - Manual: all three persona flows pass from the CloudFront URL before final sign-off.
    </acceptance_criteria>
  </task>

  <task id="06-06-03" type="execute">
    <name>Document and run en-US voice-to-profile smoke</name>
    <files>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
    </files>
    <read_first>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
      - .planning/phases/04-voice-to-profile-pipeline/04-05-PLAN.md
      - .planning/phases/05-frontend-wiring-type-extensions/05-06-PLAN.md
      - MULTI-CLOUD-ARCHITECTURE.md
    </read_first>
    <action>
In `## Smoke Flow`, add voice test steps:
1. Login as Siti from the CloudFront URL.
2. Open the elder voice-to-profile screen.
3. Select or use `en-US`.
4. Speak: `I can cook nasi lemak for lunch, ten ringgit per meal, halal, around Cyberjaya.`
5. Verify partial transcript appears while speaking.
6. Verify final response contains `type: "final"` and a `ListingDraft`.
7. Verify returned draft includes `service_offer`, `category`, `price_amount`, `price_unit`, `dietary_tags`, and `language`.

Add failure notes:
- If browser lacks AudioWorklet/WebSocket support, document whether `SpeechRecognition` fallback triggered.
- If Qwen extraction fails, capture the backend error envelope text without exposing secrets.
    </action>
    <acceptance_criteria>
      - `rg "nasi lemak|type: \"final\"|ListingDraft|service_offer|price_amount|SpeechRecognition|Qwen extraction fails" backend/docs/DEPLOYMENT_SMOKE_TEST.md` finds all strings.
      - Manual: en-US voice-to-profile returns a final listing draft from the public deployment before sign-off.
    </acceptance_criteria>
  </task>

  <task id="06-06-04" type="execute">
    <name>Capture browser console and cloud verification sign-off</name>
    <files>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
      - backend/docs/DEPLOYMENT_AWS.md
      - backend/docs/DEPLOYMENT_ALIBABA.md
    </files>
    <read_first>
      - backend/docs/DEPLOYMENT_SMOKE_TEST.md
      - backend/docs/DEPLOYMENT_AWS.md
      - backend/docs/DEPLOYMENT_ALIBABA.md
    </read_first>
    <action>
In `## Browser Console Checks`, add:
- `No CORS errors on auth, elder, requestor, companion, or voice calls.`
- `No mixed-content errors.`
- `No 404 for frontend static assets.`
- `No uncaught runtime exception during the smoke flow.`

In `## Cloud Checks`, add:
- `curl -fsS https://<alibaba-ecs-or-slb-host>/health` returns `{"status":"ok"}`.
- CloudFront distribution status is `Deployed`.
- Audio bucket CORS contains CloudFront and backend origins.
- AWS budget alerts at `$50` and `$100` exist.
- ApsaraDB migrations are at Alembic head.
- DashScope rate-limit check completed before smoke run.

In `## Sign-Off`, add fields for tester, date/time, CloudFront URL, backend origin, commit SHA, and result `PASS` or `FAIL`.
    </action>
    <acceptance_criteria>
      - `rg "No CORS errors|No mixed-content errors|CloudFront distribution status is `Deployed`|ApsaraDB migrations are at Alembic head|DashScope rate-limit check completed|result `PASS` or `FAIL`" backend/docs/DEPLOYMENT_SMOKE_TEST.md` finds all strings.
      - Manual: sign-off fields are filled with actual deployment values before Phase 6 is marked complete.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `rg "Deployment Smoke Test|Click Siti quick-login|Click Amir quick-login|ListingDraft|No CORS errors|PASS" backend/docs/DEPLOYMENT_SMOKE_TEST.md`
- `curl -fsS https://<alibaba-ecs-or-slb-host>/health` returns `{"status":"ok"}`.
- Manual browser smoke from CloudFront completes the full flow: Siti -> Amir booking -> Siti accept -> Faiz dashboard -> Siti en-US voice draft.
</verification>

<success_criteria>
- Smoke runbook exists and covers all Phase 6 requirements.
- Public CloudFront URL drives the full user journey.
- Backend, database, S3 audio, Transcribe/Qwen, CORS, and WebSocket behavior are verified from the public deployment.
- Final sign-off records actual URLs and PASS/FAIL.
</success_criteria>

<must_haves>
- Smoke test starts from CloudFront, not localhost.
- All three demo accounts are covered.
- en-US voice-to-profile returns a final `ListingDraft`.
- Browser console has no CORS errors for the five smoke-test screens.
</must_haves>
