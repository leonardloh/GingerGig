---
phase: 05
plan: 07
title: Environment configuration, no-visual-change guardrails, and final verification
type: execute
wave: 4
depends_on:
  - 05-01-PLAN.md
  - 05-02-PLAN.md
  - 05-03-PLAN.md
  - 05-04-PLAN.md
  - 05-05-PLAN.md
  - 05-06-PLAN.md
autonomous: true
requirements:
  - FE-08
  - FE-09
files_modified:
  - frontend/.env.example
  - frontend/.env.local
---

<objective>
Finalize Phase 05 by documenting `VITE_API_BASE_URL` for local FastAPI and production Alibaba ECS, then run static, contract, mock-import, and no-visual-change verification across all frontend wiring. This plan does not add new product behavior.
</objective>

<threat_model>
Frontend risks for this wave:
- **CORS/env:** `VITE_API_BASE_URL` must be a bare origin with no trailing slash and no `/api/v1`; `apiRequest` remains the only HTTP prefix owner.
- **Secrets:** `.env.local` is local/gitignored and must not contain committed secrets; production URL docs must be placeholders unless the deployed endpoint is already approved.
- **Token handling:** final grep must show no new token persistence in localStorage/sessionStorage and no Authorization header handling outside existing `http.ts` plus presigned/WS exceptions.
- **No UI drift:** final diff review must reject CSS/className/copy/layout changes unrelated to the allowed frontend wiring scope.
</threat_model>

<tasks>
  <task id="05-07-01" type="execute">
    <name>Update frontend env files for local FastAPI and Alibaba ECS production origin</name>
    <files>
      - frontend/.env.example
    </files>
    <read_first>
      - frontend/.env.example
      - frontend/.env.local
      - frontend/src/config/env.ts
      - frontend/src/services/api/http.ts
      - .planning/REQUIREMENTS.md (FE-08, FE-09)
      - MULTI-CLOUD-ARCHITECTURE.md
    </read_first>
    <action>
Edit `frontend/.env.example`:
- Keep `VITE_API_BASE_URL=http://localhost:8000` for local development.
- Clarify that the value is the backend origin only, no trailing slash and no `/api/v1`, because `http.ts` appends `/api/v1`.
- Update production comment to reference the Alibaba ECS or load balancer HTTPS origin, e.g. `# VITE_API_BASE_URL=https://<alibaba-ecs-or-slb-host>`.
- Keep `VITE_API_TIMEOUT_MS=15000`.
- Do not commit real secrets or credentials.
- Do not change `frontend/src/config/env.ts` unless required by build; default `http://localhost:8000` is already correct.
Create or update local ignored `frontend/.env.local` for developer runs:
- `VITE_API_BASE_URL=http://localhost:8000`
- `VITE_API_TIMEOUT_MS=15000`
Confirm `frontend/.env.local` is ignored by git before leaving it uncommitted.
    </action>
    <acceptance_criteria>
      - `rg "VITE_API_BASE_URL=http://localhost:8000|alibaba|/api/v1" frontend/.env.example -i` finds local value, production Alibaba guidance, and the note that `/api/v1` is appended automatically.
      - `frontend/.env.local` exists and contains `VITE_API_BASE_URL=http://localhost:8000`.
      - `git check-ignore frontend/.env.local` exits 0.
      - `rg "VITE_API_BASE_URL=.*api/v1" frontend/.env.example` returns no active assignment with `/api/v1`.
      - `rg "SECRET|KEY|TOKEN" frontend/.env.example` returns no committed secret values.
    </acceptance_criteria>
  </task>

  <task id="05-07-02" type="verify">
    <name>Run frontend static verification suite</name>
    <files>
      - frontend/package.json
    </files>
    <read_first>
      - frontend/package.json
      - .planning/phases/05-frontend-wiring-type-extensions/05-VALIDATION.md
    </read_first>
    <action>
Run from repository root:
- `cd frontend && npm run typecheck`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
Fix only Phase 05 wiring-related type/lint/build failures. Do not run formatting over unrelated files.
    </action>
    <acceptance_criteria>
      - `npm run typecheck` exits 0.
      - `npm run lint` exits 0.
      - `npm run build` exits 0.
    </acceptance_criteria>
  </task>

  <task id="05-07-03" type="verify">
    <name>Run mock-import and forbidden frontend-drift greps</name>
    <files>
      - frontend/src/prototype/PrototypeApp.jsx
      - frontend/src/prototype/OnboardingFlow.jsx
      - frontend/src/prototype/elder-screens.jsx
      - frontend/src/prototype/requestor-screens.jsx
      - frontend/src/prototype/companion-screens.jsx
      - frontend/src/services/api/http.ts
      - frontend/src/services/api/types.ts
    </files>
    <read_first>
      - .planning/phases/05-frontend-wiring-type-extensions/05-VALIDATION.md
      - .planning/phases/05-frontend-wiring-type-extensions/05-PATTERNS.md
      - frontend/src/prototype/PrototypeApp.jsx
      - frontend/src/prototype/OnboardingFlow.jsx
      - frontend/src/services/api/http.ts
      - frontend/src/services/api/types.ts
      - frontend/src/prototype/elder-screens.jsx
      - frontend/src/prototype/requestor-screens.jsx
      - frontend/src/prototype/companion-screens.jsx
    </read_first>
    <action>
Run:
- `rg "from './mock-data'|mock-data" frontend/src/prototype`
- `rg "/api/v1" frontend/src/prototype frontend/src/services/api/endpoints`
- `rg "localStorage|sessionStorage" frontend/src`
- `rg "setApiAccessToken" frontend/src`
- `rg "prototype.css|className=" frontend/src/prototype`
Expected:
- no `mock-data` imports in screen files;
- `/api/v1` appears only in `http.ts`, comments/JSDoc, or `voice.ts` WebSocket construction as planned;
- no token persistence;
- `setApiAccessToken` remains in `http.ts` and `auth.ts` only;
- className/CSS changes are reviewed manually, not blindly accepted.
    </action>
    <acceptance_criteria>
      - `rg "from './mock-data'" frontend/src/prototype` returns no matches.
      - `rg "localStorage|sessionStorage" frontend/src` returns no matches unless pre-existing unrelated code is documented.
      - `rg "setApiAccessToken" frontend/src` returns matches only in `frontend/src/services/api/http.ts` and `frontend/src/services/api/endpoints/auth.ts`.
      - `git diff -- frontend/src/prototype/prototype.css` is empty.
    </acceptance_criteria>
  </task>

  <task id="05-07-04" type="verify">
    <name>Run backend prerequisite contract tests for wired frontend paths</name>
    <files>
      - backend/tests/test_auth_demo.py
      - backend/tests/test_persona_elder.py
      - backend/tests/test_persona_requestor.py
      - backend/tests/test_persona_companion.py
      - backend/tests/test_persona_locale_and_authz.py
      - backend/tests/test_voice_contract.py
    </files>
    <read_first>
      - .planning/phases/05-frontend-wiring-type-extensions/05-VALIDATION.md
      - .planning/phases/04-voice-to-profile-pipeline/04-VALIDATION.md
      - backend/tests/test_auth_demo.py
      - backend/tests/test_persona_elder.py
      - backend/tests/test_persona_requestor.py
      - backend/tests/test_persona_companion.py
      - backend/tests/test_persona_locale_and_authz.py
      - backend/tests/test_voice_contract.py
    </read_first>
    <action>
Run:
- `cd backend && uv run pytest tests/test_auth_demo.py tests/test_persona_elder.py tests/test_persona_requestor.py tests/test_persona_companion.py tests/test_persona_locale_and_authz.py -q`
- If 05-06 executed, also run `cd backend && uv run pytest tests/test_voice_qwen.py tests/test_voice_streaming.py tests/test_voice_batch.py tests/test_voice_contract.py tests/test_no_forbidden_imports.py -q`
If Phase 4 is still incomplete, mark ElderVoice verification as blocked and do not mark FE-07 complete.
    </action>
    <acceptance_criteria>
      - Persona/auth backend tests exit 0.
      - Voice backend tests exit 0 before FE-07 is considered done, or the plan records the Phase 4 blocker.
    </acceptance_criteria>
  </task>

  <task id="05-07-05" type="verify">
    <name>Manual no-visual-change and end-to-end smoke pass</name>
    <files>
      - frontend/src/prototype/PrototypeApp.jsx
      - frontend/src/prototype/OnboardingFlow.jsx
      - frontend/src/prototype/elder-screens.jsx
      - frontend/src/prototype/requestor-screens.jsx
      - frontend/src/prototype/companion-screens.jsx
      - frontend/.env.example
    </files>
    <read_first>
      - .planning/phases/05-frontend-wiring-type-extensions/05-VALIDATION.md
      - .planning/ROADMAP.md (Phase 5 success criteria)
      - .planning/REQUIREMENTS.md (FE-01 through FE-09)
      - frontend/src/prototype/PrototypeApp.jsx
      - frontend/src/prototype/OnboardingFlow.jsx
      - frontend/src/prototype/elder-screens.jsx
      - frontend/src/prototype/requestor-screens.jsx
      - frontend/src/prototype/companion-screens.jsx
      - frontend/.env.example
    </read_first>
    <action>
With backend running, migrated, and seeded:
- Start backend: `cd backend && uv run uvicorn app.main:app --reload`.
- Start frontend: `cd frontend && npm run dev`.
- Smoke Siti: quick-login chip, elder dashboard/listings/profile render API data, accept/decline works, voice flow if Phase 4 gate passed.
- Smoke Amir: quick-login chip, requestor home/search/detail/profile render API data, detail uses listing ids and displays menu/days/reviews.
- Smoke Faiz: quick-login chip, companion dashboard/alerts/timeline render using companion endpoints; preference toggles call PUT and no elder-only routes appear in Network.
- Compare before/after visually for login, onboarding, elder dashboard/listings/voice/profile, requestor home/search/detail/profile, companion dashboard/alerts/profile.
- Review browser console for CORS errors, unhandled promise rejections, and failed double-prefix paths.
    </action>
    <acceptance_criteria>
      - All three quick-login chips complete end-to-end against backend auth.
      - Browser Network shows `/api/v1/auth/login`, `/api/v1/auth/me`, persona endpoint calls, and no `/api/v1/api/v1`.
      - No CSS file changes and no intentional copy/class/layout changes in git diff.
      - Any remaining open gate is documented: Phase 4 voice incomplete or companion upcoming bookings lacking a companion-safe endpoint.
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `cd frontend && npm run typecheck && npm run lint && npm run build`
- `rg "from './mock-data'" frontend/src/prototype`
- `rg "localStorage|sessionStorage" frontend/src`
- `cd backend && uv run pytest tests/test_auth_demo.py tests/test_persona_elder.py tests/test_persona_requestor.py tests/test_persona_companion.py tests/test_persona_locale_and_authz.py -q`
- If voice gate passed: `cd backend && uv run pytest tests/test_voice_qwen.py tests/test_voice_streaming.py tests/test_voice_batch.py tests/test_voice_contract.py tests/test_no_forbidden_imports.py -q`
</verification>

<success_criteria>
- `VITE_API_BASE_URL` docs/config are correct for local dev and production Alibaba ECS origin.
- Static frontend checks and relevant backend contract tests pass.
- No prototype screen imports `mock-data.js`.
- `apiRequest`, bearer token semantics, `ApiError`, and `/api/v1` prefix ownership remain unchanged.
- Manual smoke confirms no visual drift and all three personas load through the typed API client.
</success_criteria>
