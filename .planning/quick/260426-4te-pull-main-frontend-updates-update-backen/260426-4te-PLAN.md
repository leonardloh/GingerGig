---
quick_id: 260426-4te
slug: pull-main-frontend-updates-update-backen
status: complete
created: 2026-04-25
---

# Quick Task 260426-4te: Pull Main Frontend Updates And Verify Backend API

## Objective

Merge the latest `origin/main` frontend changes into the `backend` branch, update the real backend/API contract where the new frontend expects additional routes or fields, and verify the app runs locally before Phase 6 deployment work.

## Tasks

1. Pull `origin/main` into `backend` and resolve frontend/API conflicts.
2. Adapt frontend API endpoint modules to translate FastAPI DTOs into the merged UI shapes without bypassing the backend during demo login.
3. Add a local KYC route contract so onboarding can complete without cloud S3/Textract/Rekognition during local development.
4. Run backend, frontend, and local smoke verification.

## Verification

- `cd backend && uv run pytest -q`
- `cd frontend && npm run typecheck`
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
- Local smoke on alternate ports because `8000` was already occupied:
  - backend `http://127.0.0.1:8001`
  - frontend `http://127.0.0.1:5175`
